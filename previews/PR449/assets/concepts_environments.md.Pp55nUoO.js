import{_ as t,o as i,c as n,ao as a}from"./chunks/framework.DA_3JxoV.js";const m=JSON.parse('{"title":"Environments","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/environments.md","filePath":"concepts/environments.md","lastUpdated":null}'),s={name:"concepts/environments.md"};function o(r,e,h,l,d,c){return i(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="concept_environments" tabindex="-1">Environments <a class="header-anchor" href="#concept_environments" aria-label="Permalink to &quot;Environments {#concept_environments}&quot;">​</a></h1><p>Almost every MPS algorithm spends most of its time contracting the same tensor network over and over. In DMRG, optimizing the tensor on one site requires the sum of all Hamiltonian contributions sitting to its left and to its right; in time evolution the same partial contractions reappear at every step. Recomputing them from scratch each time would be wasteful, because moving attention from one site to a neighbour changes only a little of the network. The <em>environment</em> objects are what let MPSKit avoid that waste.</p><p>This page explains what environments are and why they exist, so that the optional <code>environments</code> argument that appears throughout the API stops looking like a mystery. It is about understanding, not tuning: for the mechanics of a particular algorithm follow the links into the how-to pages.</p><h2 id="What-an-environment-is" tabindex="-1">What an environment is <a class="header-anchor" href="#What-an-environment-is" aria-label="Permalink to &quot;What an environment is {#What-an-environment-is}&quot;">​</a></h2><p>An environment is a partially contracted piece of a tensor network — the part that does not change when you shift your focus by one site. Consider the network whose value an algorithm ultimately wants: a state <code>below</code> (the bra), an operator, and a state <code>above</code> (the ket), all contracted together. Fixing attention on a single site splits that network into three parts: the tensor at the site itself, everything to its left, and everything to its right. The left and right parts are exactly the <em>left environment</em> and <em>right environment</em> of that site.</p><p>The key observation is that these two blocks are shared between neighbouring sites. The left environment at site <code>i+1</code> is the left environment at site <code>i</code> with a single extra column contracted onto it. So once you have paid to build the environment at one site, advancing to the next is cheap: you add one new contribution instead of recontracting the whole chain. Caching the environments and reusing them across a sweep is what turns an algorithm that would be quadratic in the system size into a linear one.</p><p>In MPSKit these cached blocks live in an environment object, constructed with the exported <a href="/MPSKit.jl/previews/PR449/lib/lib#MPSKit.environments"><code>environments</code></a> function. The canonical form sandwiches an operator between two states,</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MPSKit, MPSKitModels, TensorKit</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">state </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> FiniteMPS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ℂ</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ℂ</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">H </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> transverse_field_ising</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FiniteChain</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); g </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">envs </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> environments</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(state, H, state)</span></span></code></pre></div><p>while the two-argument form <code>environments(below, above)</code> builds the operator-free <em>overlap</em> environments between two states. The individual blocks are then queried with the exported <a href="/MPSKit.jl/previews/PR449/lib/environments#MPSKit.leftenv-lib-environments"><code>leftenv</code></a> and <a href="/MPSKit.jl/previews/PR449/lib/environments#MPSKit.rightenv-lib-environments"><code>rightenv</code></a> functions,</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GL </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> leftenv</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(envs, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, state)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># everything to the left of site 10</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">GR </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rightenv</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(envs, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, state)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># everything to the right of site 10</span></span></code></pre></div><p>each of which returns a tensor gauge-compatible with the state tensor at that site, ready to be contracted onto it.</p><h2 id="Why-you-rarely-build-them-yourself" tabindex="-1">Why you rarely build them yourself <a class="header-anchor" href="#Why-you-rarely-build-them-yourself" aria-label="Permalink to &quot;Why you rarely build them yourself {#Why-you-rarely-build-them-yourself}&quot;">​</a></h2><p>Most of the time you never touch an environment object at all. The high-level entry points — <a href="/MPSKit.jl/previews/PR449/lib/groundstate#MPSKit.find_groundstate-lib-groundstate"><code>find_groundstate</code></a>, <a href="/MPSKit.jl/previews/PR449/lib/lib#MPSKit.timestep"><code>timestep</code></a>, <a href="/MPSKit.jl/previews/PR449/lib/lib#MPSKit.excitations"><code>excitations</code></a>, and the rest — build whatever environments they need internally. What they also do, uniformly, is accept an <em>optional</em> environments argument and return an updated environment object alongside their main result.</p><p>That return value is the reason to care about environments even when you never construct one. Handing the environments from one call into the next lets the algorithm reuse the cached blocks instead of rebuilding them from nothing. For iterated procedures such as time evolution — where each step starts from a state only slightly different from the last — feeding the updated environments back in every step avoids repeating work that the previous step already did. The <a href="/MPSKit.jl/previews/PR449/howto/time_evolution#howto_time_evolution">Time evolution</a> how-to shows this threading pattern in a concrete recipe.</p><h2 id="Finite-environments-and-the-cache" tabindex="-1">Finite environments and the <code>===</code> cache <a class="header-anchor" href="#Finite-environments-and-the-cache" aria-label="Permalink to &quot;Finite environments and the \`===\` cache {#Finite-environments-and-the-cache}&quot;">​</a></h2><p>For a finite state the environment object manages its own validity automatically, and understanding how is worthwhile because it comes with one sharp edge.</p><p>When it computes a left environment, the cache records <em>which</em> state tensors it contracted to get there — the gauged tensors of <code>state</code> up to that site. On a later query it compares the tensors it would need now against the ones it used before, testing them with Julia&#39;s identity operator <code>===</code>. If they are the same objects, the cached block is still valid and is returned immediately. If some differ, the cache recomputes only the affected part of the network and updates its record. This is what makes repeated queries during a sweep cheap: the first <code>leftenv</code> at a far site pays for the full contraction, and neighbouring queries reuse almost all of it.</p><p>The sharp edge is that <code>===</code> tests object identity, not numerical equality. If you mutate a state tensor <em>in place</em> — changing its data while keeping the same object — the cache still sees the same object under <code>===</code> and concludes, wrongly, that its stored environment is still valid. It will then hand back a block computed from the old data. Building a <em>new</em> tensor and assigning it into the state is fine, because that is a different object and the <code>===</code> check catches it; only in-place mutation defeats the mechanism. Because algorithms that use the public API replace tensors rather than mutating them, this is rarely a problem in normal use, but it is the thing to suspect if a hand-written routine that mutates tensors starts returning stale results.</p><div class="warning custom-block"><p class="custom-block-title">In-place mutation is invisible to the cache</p><p>The finite-environment cache detects changes by object identity (<code>===</code>), so mutating a state tensor in place leaves the cache convinced its stored environment is still current. The internal, non-exported helper <code>MPSKit.poison!(envs, i)</code> marks the dependencies at site <code>i</code> as stale so the next query recomputes them; needing it is a sign that a tensor was mutated in place rather than replaced.</p></div><h2 id="Infinite-environments" tabindex="-1">Infinite environments <a class="header-anchor" href="#Infinite-environments" aria-label="Permalink to &quot;Infinite environments {#Infinite-environments}&quot;">​</a></h2><p>Infinite environments serve the same role but are computed differently, and the difference matters for how you use them.</p><p>A finite chain has genuine boundaries, so its environments can be built by contracting inward from the ends in a finite number of steps. An infinite chain has no ends. Its environments are instead the fixed points of the transfer operator — the object you get by contracting one repeating unit cell — and finding a fixed point means solving a linear or eigenvalue problem. Those problems are solved <em>iteratively</em>, to a finite tolerance, rather than by an exact finite contraction. Building an infinite environment is therefore a small numerical solve, and its result is only as accurate as the tolerance of that solve.</p><p>The precision of that solve is controlled by the <code>tol</code>, <code>maxiter</code>, and <code>krylovdim</code> keyword arguments to <a href="/MPSKit.jl/previews/PR449/lib/lib#MPSKit.environments"><code>environments</code></a>, which configure the underlying iterative solver — an Arnoldi eigensolver for a transfer-matrix fixed point, or GMRES for the linear problem that arises with an <code>InfiniteMPOHamiltonian</code>. It is fixed when the environments are built, rather than read from or written to the environment object afterwards.</p><p>The second difference is that infinite environments are <strong>not</strong> recomputed automatically. The finite cache re-validates itself against the current state on every query; the infinite one does not. If the state changes, the stored fixed points no longer correspond to it, and there is no automatic re-solve. Bringing an infinite environment up to date for a changed state is an explicit step, handled internally by the non-exported <code>MPSKit.recalculate!</code>.In practice the high-level algorithms perform this recomputation for you as part of their own iteration, which is again why threading the returned environments through successive calls is the efficient pattern.</p><h2 id="Where-to-go-next" tabindex="-1">Where to go next <a class="header-anchor" href="#Where-to-go-next" aria-label="Permalink to &quot;Where to go next {#Where-to-go-next}&quot;">​</a></h2><p>For the full signatures and docstrings of the environment functions, see <a href="/MPSKit.jl/previews/PR449/lib/lib#MPSKit.environments"><code>environments</code></a>, <a href="/MPSKit.jl/previews/PR449/lib/environments#MPSKit.leftenv-lib-environments"><code>leftenv</code></a>, and <a href="/MPSKit.jl/previews/PR449/lib/environments#MPSKit.rightenv-lib-environments"><code>rightenv</code></a> in the library reference. For the algorithm-facing side — how ground-state and time-evolution routines consume and return environments — see <a href="/MPSKit.jl/previews/PR449/howto/groundstate_algorithms#howto_groundstate_algorithms">Ground-state algorithms</a> and <a href="/MPSKit.jl/previews/PR449/howto/time_evolution#howto_time_evolution">Time evolution</a>.</p>`,26)])])}const u=t(s,[["render",o]]);export{m as __pageData,u as default};
