import{_ as a,o as t,c as r,ao as s}from"./chunks/framework.DA_3JxoV.js";const m=JSON.parse('{"title":"The parallelism model","description":"","frontmatter":{},"headers":[],"relativePath":"concepts/parallelism_model.md","filePath":"concepts/parallelism_model.md","lastUpdated":null}'),o={name:"concepts/parallelism_model.md"};function l(i,e,h,n,d,p){return t(),r("div",null,[...e[0]||(e[0]=[s('<h1 id="concept_parallelism_model" tabindex="-1">The parallelism model <a class="header-anchor" href="#concept_parallelism_model" aria-label="Permalink to &quot;The parallelism model {#concept_parallelism_model}&quot;">​</a></h1><p>Julia has excellent <a href="https://julialang.org/blog/2019/07/multithreading/" target="_blank" rel="noreferrer">parallelism infrastructure</a>, but there is a caveat that touches every algorithm in MPSKit: Julia&#39;s own threads do not compose cleanly with the threads that BLAS uses internally for linear algebra. Since <code>gemm</code> (general matrix-matrix multiplication) is a core routine throughout MPSKit, this interaction has a real effect on performance.</p><p>This page explains the model behind the settings, so that the recipes on <a href="/MPSKit.jl/previews/PR449/howto/parallelism_gpu#howto_parallelism_gpu">Parallelism and GPU support</a> are more than a list of magic incantations.</p><h2 id="Julia-threads-versus-BLAS-threads" tabindex="-1">Julia threads versus BLAS threads <a class="header-anchor" href="#Julia-threads-versus-BLAS-threads" aria-label="Permalink to &quot;Julia threads versus BLAS threads {#Julia-threads-versus-BLAS-threads}&quot;">​</a></h2><p>Much of the confusion here comes from the fact that BLAS threading behaviour is not consistent between vendors, and that performance depends strongly on the hardware, the specifics of the problem, and the availability of resources such as total memory and memory bandwidth. There is no one-size-fits-all setting, which is why the how-to page frames its advice as starting points to be measured rather than guarantees.</p><p>The two vendors most commonly used with Julia treat the BLAS thread count differently. With OpenBLAS (the default), the configured number of BLAS threads is the <strong>total</strong> size of a single thread pool that is shared by all Julia threads: 4 Julia threads and 4 BLAS threads means all 4 Julia threads draw from the same pool of 4 BLAS threads. Setting the BLAS thread count to <code>1</code> instead frees OpenBLAS to run its work on the Julia threads themselves, so that MPSKit&#39;s Julia-level parallelism is the thing that scales.</p><p>With <a href="https://github.com/JuliaLinearAlgebra/MKL.jl" target="_blank" rel="noreferrer">MKL.jl</a>, which often outperforms OpenBLAS, the count is instead the number of threads spawned by <strong>each</strong> Julia thread: 4 Julia threads with 4 BLAS threads each gives 16 BLAS threads in total. Getting this wrong oversubscribes the physical cores — more software threads than hardware can run — which degrades rather than improves performance.</p><h2 id="Where-MPSKit-parallelizes" tabindex="-1">Where MPSKit parallelizes <a class="header-anchor" href="#Where-MPSKit-parallelizes" aria-label="Permalink to &quot;Where MPSKit parallelizes {#Where-MPSKit-parallelizes}&quot;">​</a></h2><p>When Julia is started with more than one thread, MPSKit uses <a href="https://juliafolds2.github.io/OhMyThreads.jl/stable/" target="_blank" rel="noreferrer">OhMyThreads.jl</a> to parallelize its algorithms wherever possible. In practice this happens where a unit cell (or a chain of sites) lets local updates run independently: the work is distributed across the sites of the system, with the tensor at each site updated in parallel. This is exactly why setting the BLAS thread count to <code>1</code> on OpenBLAS tends to help: it keeps the Julia threads free to work through the sites, rather than contending with a shared BLAS pool.</p><p>The amount of speedup you can expect therefore tracks how much independent per-site work an algorithm exposes.</p><h2 id="Parallelism-over-symmetry-sectors" tabindex="-1">Parallelism over symmetry sectors <a class="header-anchor" href="#Parallelism-over-symmetry-sectors" aria-label="Permalink to &quot;Parallelism over symmetry sectors {#Parallelism-over-symmetry-sectors}&quot;">​</a></h2><p>There is a second, orthogonal layer of parallelism for tensors that carry an internal symmetry. Such tensors are block-diagonal over their symmetry sectors, and the work can be spread across those blocks. This is handled by <a href="https://quantumkithub.github.io/TensorKit.jl/stable/" target="_blank" rel="noreferrer">TensorKit</a> at the level of the individual tensor operations, below MPSKit&#39;s site-level parallelism, so the two layers are independent of one another.</p><h2 id="Why-memory-pressure-arises" tabindex="-1">Why memory pressure arises <a class="header-anchor" href="#Why-memory-pressure-arises" aria-label="Permalink to &quot;Why memory pressure arises {#Why-memory-pressure-arises}&quot;">​</a></h2><p>The same task-based parallelism that speeds MPSKit up can also drive its memory usage high. The algorithms spawn tasks in a nested fashion, and each of those tasks allocates and deallocates a fair amount of memory in a tight loop. This can produce enough garbage, quickly enough, that the garbage collector cannot keep up; in the worst case memory is exhausted and an <code>OutOfMemory</code> error is thrown before the garbage can be cleared.</p><p>The most memory-intensive step is reportedly the application of the <code>derivatives</code> — the effective local operators built during the sweeps — which is why the practical mitigation is to disable MPSKit&#39;s multithreading there. The concrete recipe for doing so is on <a href="/MPSKit.jl/previews/PR449/howto/parallelism_gpu#howto_parallelism_gpu">Parallelism and GPU support</a>.</p>',15)])])}const u=a(o,[["render",l]]);export{m as __pageData,u as default};
